# Copia questo file come .env e compila i valori reali
# NON committare mai il file .env su Git!

DB_HOST=localhost
DB_NAME=safeschool
DB_USER=root
DB_PASS=

# Genera con: php -r "echo bin2hex(random_bytes(32));"
VAULT_KEY=sostituisci_con_64_caratteri_hex_casuali
